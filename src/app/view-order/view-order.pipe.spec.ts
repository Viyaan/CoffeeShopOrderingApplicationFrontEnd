import { ViewOrderPipe } from './view-order.pipe';

describe('ViewOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
